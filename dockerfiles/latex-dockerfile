FROM ubuntu:20.04

ENV DEBIAN_FRONTEND=noninteractive

RUN apt update && \
    apt-get install -qy \
        # latex \
        texlive-full \
        # other tools \
        make gnuplot graphviz && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

